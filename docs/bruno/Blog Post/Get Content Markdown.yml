info:
  name: Get Content Markdown
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/posts/:slug/markdown"
  params:
    - name: slug
      value: teste
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
