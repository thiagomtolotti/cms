info:
  name: Get Content
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/posts/:slug"
  params:
    - name: slug
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
