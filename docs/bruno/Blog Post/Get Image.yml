info:
  name: Get Image
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/posts/:slug/image"
  params:
    - name: slug
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
