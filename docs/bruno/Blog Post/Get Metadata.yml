info:
  name: Get Metadata
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/posts/:slug/metadata"
  params:
    - name: slug
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
