info:
  name: Create Transaction
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/finance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
