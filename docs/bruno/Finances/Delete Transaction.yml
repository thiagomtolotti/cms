info:
  name: Delete Transaction
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{api_url}}/finance/:id"
  params:
    - name: id
      value: "1234"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
