info:
  name: List Transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/finance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
