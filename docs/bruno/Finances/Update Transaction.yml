info:
  name: Update Transaction
  type: http
  seq: 3

http:
  method: PUT
  url: "{{api_url}}/transaction:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
